# To be /brainstorming

- GlobalRef and NSRef are similar concepts. GlobalRef actually combine two things, module reference and global variable reference. And I think global variable reference part is very similar to namespace reference. The idea is to split GlobalRef into ModuleRef and GlobalRef(global var ref part only) and make NSRef as super of GlobalRef.
- Alternative key support for a lythonic.state
