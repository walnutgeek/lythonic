# Event-Driven DAG Triggers Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add event-driven DAG triggers — poll and push types — with declarative definitions in the namespace, activation state in SQLite, and a `TriggerManager` that coordinates execution.

**Architecture:** `TriggerDef` (Pydantic model) is registered in `Namespace` via `register_trigger()`. `TriggerStore` manages activation/event state in a dedicated SQLite DB. `TriggerManager` bridges namespace definitions to runtime: `activate()` creates DB records, `fire()` runs push triggers, `start()`/`stop()` runs a background loop for poll triggers.

**Tech Stack:** Python 3.11+, asyncio, Pydantic, SQLite, pytest

---

## File Structure

| File | Responsibility |
|---|---|
| `src/lythonic/compose/trigger.py` | `TriggerDef` model, `TriggerStore` (SQLite), `TriggerManager` |
| `src/lythonic/compose/namespace.py` | `register_trigger()`, `get_trigger()` on `Namespace` |
| `tests/test_trigger.py` | All trigger tests |
| `docs/reference/compose-trigger.md` | mkdocstrings reference page |
| `mkdocs.yml` | Add reference page to nav |

---

### Task 1: TriggerDef model and Namespace integration

**Files:**
- Create: `src/lythonic/compose/trigger.py`
- Modify: `src/lythonic/compose/namespace.py`
- Test: `tests/test_trigger.py`

- [ ] **Step 1: Write failing tests for TriggerDef and namespace registration**

Create `tests/test_trigger.py`:

```python
from __future__ import annotations

import tests.test_trigger as this_module
from lythonic.periodic import Interval


def _poll_fn() -> dict[str, str] | None:  # pyright: ignore[reportUnusedFunction]
    return {"data": "test"}


def test_register_trigger_poll():
    from lythonic.compose.namespace import Namespace

    ns = Namespace()
    ns.register_trigger(
        name="daily_etl",
        dag_nsref="pipelines:etl",
        trigger_type="poll",
        interval=Interval.from_string("1D"),
    )
    td = ns.get_trigger("daily_etl")
    assert td.name == "daily_etl"
    assert td.dag_nsref == "pipelines:etl"
    assert td.trigger_type == "poll"
    assert td.interval is not None


def test_register_trigger_push():
    from lythonic.compose.namespace import Namespace

    ns = Namespace()
    ns.register_trigger(
        name="webhook",
        dag_nsref="pipelines:handle",
        trigger_type="push",
    )
    td = ns.get_trigger("webhook")
    assert td.trigger_type == "push"
    assert td.interval is None


def test_register_trigger_poll_with_fn():
    from lythonic.compose.namespace import Namespace

    ns = Namespace()
    ns.register_trigger(
        name="new_orders",
        dag_nsref="pipelines:orders",
        trigger_type="poll",
        poll_fn=this_module._poll_fn,  # pyright: ignore[reportPrivateUsage]
        interval=Interval.from_string("5m"),
    )
    td = ns.get_trigger("new_orders")
    assert td.poll_fn is not None


def test_register_trigger_duplicate_raises():
    from lythonic.compose.namespace import Namespace

    ns = Namespace()
    ns.register_trigger(name="t1", dag_nsref="p:d", trigger_type="push")
    try:
        ns.register_trigger(name="t1", dag_nsref="p:d", trigger_type="push")
        raise AssertionError("Expected ValueError")
    except ValueError as e:
        assert "already" in str(e).lower()


def test_get_trigger_missing_raises():
    from lythonic.compose.namespace import Namespace

    ns = Namespace()
    try:
        ns.get_trigger("nonexistent")
        raise AssertionError("Expected KeyError")
    except KeyError:
        pass


def test_register_trigger_poll_requires_interval():
    from lythonic.compose.namespace import Namespace

    ns = Namespace()
    try:
        ns.register_trigger(name="bad", dag_nsref="p:d", trigger_type="poll")
        raise AssertionError("Expected ValueError")
    except ValueError as e:
        assert "interval" in str(e).lower()
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `uv run pytest tests/test_trigger.py -v`
Expected: FAIL — `TriggerDef`, `register_trigger`, `get_trigger` don't exist.

- [ ] **Step 3: Implement TriggerDef and Namespace methods**

Create `src/lythonic/compose/trigger.py`:

```python
"""
Trigger: Event-driven DAG execution.

Provides `TriggerDef` for declarative trigger definitions,
`TriggerStore` for activation state persistence, and
`TriggerManager` for runtime coordination.
"""

from __future__ import annotations

from collections.abc import Callable
from typing import Any

from pydantic import BaseModel, model_validator

from lythonic.periodic import Interval


class TriggerDef(BaseModel):
    """
    Declarative trigger definition. Registered in a `Namespace` via
    `register_trigger()`. Does not start anything — purely metadata.
    """

    name: str
    dag_nsref: str
    trigger_type: str  # "poll" or "push"
    interval: Interval | None = None
    poll_fn: Callable[[], Any] | None = None

    model_config = {"arbitrary_types_allowed": True}

    @model_validator(mode="after")
    def _validate_trigger(self) -> TriggerDef:
        if self.trigger_type == "poll" and self.interval is None:
            raise ValueError(f"Trigger '{self.name}': poll triggers require an interval")
        return self
```

In `src/lythonic/compose/namespace.py`, add to the `Namespace` class (after `query()`, before `__getattr__`):

```python
    def register_trigger(
        self,
        name: str,
        dag_nsref: str,
        trigger_type: str,
        interval: Interval | None = None,
        poll_fn: Callable[[], Any] | None = None,
    ) -> TriggerDef:
        """Register a trigger definition. Purely declarative."""
        from lythonic.compose.trigger import TriggerDef

        if not hasattr(self, "_triggers"):
            self._triggers: dict[str, TriggerDef] = {}
        if name in self._triggers:
            raise ValueError(f"Trigger '{name}' already registered")

        td = TriggerDef(
            name=name,
            dag_nsref=dag_nsref,
            trigger_type=trigger_type,
            interval=interval,
            poll_fn=poll_fn,
        )
        self._triggers[name] = td
        return td

    def get_trigger(self, name: str) -> TriggerDef:
        """Retrieve a trigger definition by name. Raises `KeyError` if not found."""
        if not hasattr(self, "_triggers") or name not in self._triggers:
            raise KeyError(f"Trigger '{name}' not found")
        return self._triggers[name]
```

Also add `from lythonic.periodic import Interval` to the imports and initialize `_triggers` in `__init__`:

```python
    def __init__(self) -> None:
        self._branches = {}
        self._leaves = {}
        self._triggers: dict[str, TriggerDef] = {}
```

(Use a forward reference or lazy import for `TriggerDef` in the type annotation to avoid circular imports.)

- [ ] **Step 4: Run tests to verify they pass**

Run: `uv run pytest tests/test_trigger.py -v`
Expected: All PASS.

- [ ] **Step 5: Run full test suite and lint**

Run: `make lint && make test`
Expected: All pass.

- [ ] **Step 6: Commit**

```bash
git add src/lythonic/compose/trigger.py src/lythonic/compose/namespace.py tests/test_trigger.py
git commit -m "feat: add TriggerDef model and Namespace.register_trigger()/get_trigger()"
```

---

### Task 2: TriggerStore (SQLite persistence)

**Files:**
- Modify: `src/lythonic/compose/trigger.py`
- Test: `tests/test_trigger.py`

- [ ] **Step 1: Write failing tests for TriggerStore**

Add to `tests/test_trigger.py`:

```python
import tempfile
from pathlib import Path


def test_trigger_store_activate():
    from lythonic.compose.trigger import TriggerDef, TriggerStore

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        td = TriggerDef(
            name="daily",
            dag_nsref="p:etl",
            trigger_type="poll",
            interval=Interval.from_string("1D"),
        )
        store.activate(td)

        activation = store.get_activation("daily")
        assert activation is not None
        assert activation["name"] == "daily"
        assert activation["dag_nsref"] == "p:etl"
        assert activation["status"] == "active"


def test_trigger_store_deactivate():
    from lythonic.compose.trigger import TriggerDef, TriggerStore

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        td = TriggerDef(name="t1", dag_nsref="p:d", trigger_type="push")
        store.activate(td)
        store.deactivate("t1")

        activation = store.get_activation("t1")
        assert activation is not None
        assert activation["status"] == "disabled"


def test_trigger_store_record_event():
    from lythonic.compose.trigger import TriggerDef, TriggerStore

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        td = TriggerDef(name="t1", dag_nsref="p:d", trigger_type="push")
        store.activate(td)

        store.record_event("t1", payload={"key": "value"}, run_id="run-123", status="completed")

        events = store.get_events("t1")
        assert len(events) == 1
        assert events[0]["trigger_name"] == "t1"
        assert events[0]["run_id"] == "run-123"
        assert events[0]["status"] == "completed"


def test_trigger_store_get_active_poll_triggers():
    from lythonic.compose.trigger import TriggerDef, TriggerStore

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        store.activate(
            TriggerDef(name="poll1", dag_nsref="p:a", trigger_type="poll", interval=Interval.from_string("1D"))
        )
        store.activate(
            TriggerDef(name="push1", dag_nsref="p:b", trigger_type="push")
        )
        store.activate(
            TriggerDef(name="poll2", dag_nsref="p:c", trigger_type="poll", interval=Interval.from_string("1h"))
        )
        store.deactivate("poll2")

        active_polls = store.get_active_poll_triggers()
        assert len(active_polls) == 1
        assert active_polls[0]["name"] == "poll1"


def test_trigger_store_update_last_run():
    from lythonic.compose.trigger import TriggerDef, TriggerStore

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        td = TriggerDef(
            name="t1", dag_nsref="p:d", trigger_type="poll", interval=Interval.from_string("1D")
        )
        store.activate(td)
        store.update_last_run("t1", run_id="run-1")

        activation = store.get_activation("t1")
        assert activation is not None
        assert activation["last_run_id"] == "run-1"
        assert activation["last_run_at"] is not None
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `uv run pytest tests/test_trigger.py -k "store" -v`
Expected: FAIL — `TriggerStore` doesn't exist.

- [ ] **Step 3: Implement TriggerStore**

Add to `src/lythonic/compose/trigger.py`:

```python
import json
import time
import uuid
from pathlib import Path

from lythonic.state import execute_sql, open_sqlite_db

_TRIGGER_ACTIVATIONS_DDL = """\
CREATE TABLE IF NOT EXISTS trigger_activations (
    name TEXT PRIMARY KEY,
    dag_nsref TEXT NOT NULL,
    trigger_type TEXT NOT NULL,
    status TEXT NOT NULL,
    last_run_at REAL,
    next_run_at REAL,
    last_run_id TEXT,
    created_at REAL NOT NULL,
    config_json TEXT
)"""

_TRIGGER_EVENTS_DDL = """\
CREATE TABLE IF NOT EXISTS trigger_events (
    event_id TEXT PRIMARY KEY,
    trigger_name TEXT NOT NULL,
    fired_at REAL NOT NULL,
    run_id TEXT,
    payload_json TEXT,
    status TEXT NOT NULL,
    FOREIGN KEY (trigger_name) REFERENCES trigger_activations(name)
)"""


class TriggerStore:
    """SQLite-backed storage for trigger activations and events."""

    db_path: Path

    def __init__(self, db_path: Path) -> None:
        self.db_path = db_path
        self.db_path.parent.mkdir(parents=True, exist_ok=True)
        with open_sqlite_db(self.db_path) as conn:
            cursor = conn.cursor()
            execute_sql(cursor, _TRIGGER_ACTIVATIONS_DDL)
            execute_sql(cursor, _TRIGGER_EVENTS_DDL)
            conn.commit()

    def activate(self, trigger_def: TriggerDef) -> None:
        """Create or update an activation record from a trigger definition."""
        config = {}
        if trigger_def.interval is not None:
            config["interval"] = str(trigger_def.interval)
        if trigger_def.poll_fn is not None:
            from lythonic import GlobalRef

            config["poll_fn"] = str(GlobalRef(trigger_def.poll_fn))

        with open_sqlite_db(self.db_path) as conn:
            cursor = conn.cursor()
            execute_sql(
                cursor,
                "INSERT OR REPLACE INTO trigger_activations "
                "(name, dag_nsref, trigger_type, status, created_at, config_json) "
                "VALUES (?, ?, ?, ?, ?, ?)",
                (
                    trigger_def.name,
                    trigger_def.dag_nsref,
                    trigger_def.trigger_type,
                    "active",
                    time.time(),
                    json.dumps(config),
                ),
            )
            conn.commit()

    def deactivate(self, name: str) -> None:
        """Set activation status to disabled."""
        with open_sqlite_db(self.db_path) as conn:
            cursor = conn.cursor()
            execute_sql(
                cursor,
                "UPDATE trigger_activations SET status = ? WHERE name = ?",
                ("disabled", name),
            )
            conn.commit()

    def get_activation(self, name: str) -> dict[str, Any] | None:
        """Get activation record by name."""
        with open_sqlite_db(self.db_path) as conn:
            cursor = conn.cursor()
            execute_sql(
                cursor,
                "SELECT * FROM trigger_activations WHERE name = ?",
                (name,),
            )
            row = cursor.fetchone()
            if row is None:
                return None
            cols = [d[0] for d in cursor.description]
            return dict(zip(cols, row))

    def get_active_poll_triggers(self) -> list[dict[str, Any]]:
        """Get all active poll trigger activations."""
        with open_sqlite_db(self.db_path) as conn:
            cursor = conn.cursor()
            execute_sql(
                cursor,
                "SELECT * FROM trigger_activations WHERE trigger_type = ? AND status = ?",
                ("poll", "active"),
            )
            cols = [d[0] for d in cursor.description]
            return [dict(zip(cols, row)) for row in cursor.fetchall()]

    def record_event(
        self,
        trigger_name: str,
        payload: dict[str, Any] | None = None,
        run_id: str | None = None,
        status: str = "pending",
    ) -> str:
        """Record a trigger event. Returns the event_id."""
        event_id = str(uuid.uuid4())
        with open_sqlite_db(self.db_path) as conn:
            cursor = conn.cursor()
            execute_sql(
                cursor,
                "INSERT INTO trigger_events "
                "(event_id, trigger_name, fired_at, run_id, payload_json, status) "
                "VALUES (?, ?, ?, ?, ?, ?)",
                (
                    event_id,
                    trigger_name,
                    time.time(),
                    run_id,
                    json.dumps(payload) if payload else None,
                    status,
                ),
            )
            conn.commit()
        return event_id

    def get_events(self, trigger_name: str) -> list[dict[str, Any]]:
        """Get all events for a trigger."""
        with open_sqlite_db(self.db_path) as conn:
            cursor = conn.cursor()
            execute_sql(
                cursor,
                "SELECT * FROM trigger_events WHERE trigger_name = ? ORDER BY fired_at DESC",
                (trigger_name,),
            )
            cols = [d[0] for d in cursor.description]
            return [dict(zip(cols, row)) for row in cursor.fetchall()]

    def update_last_run(self, name: str, run_id: str) -> None:
        """Update the last run info for an activation."""
        with open_sqlite_db(self.db_path) as conn:
            cursor = conn.cursor()
            execute_sql(
                cursor,
                "UPDATE trigger_activations SET last_run_at = ?, last_run_id = ? WHERE name = ?",
                (time.time(), run_id, name),
            )
            conn.commit()
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `uv run pytest tests/test_trigger.py -k "store" -v`
Expected: All PASS.

- [ ] **Step 5: Run full test suite and lint**

Run: `make lint && make test`
Expected: All pass.

- [ ] **Step 6: Commit**

```bash
git add src/lythonic/compose/trigger.py tests/test_trigger.py
git commit -m "feat: add TriggerStore for SQLite-backed activation and event persistence"
```

---

### Task 3: TriggerManager — push triggers and fire()

**Files:**
- Modify: `src/lythonic/compose/trigger.py`
- Test: `tests/test_trigger.py`

- [ ] **Step 1: Write failing tests for push trigger execution**

Add helpers and tests to `tests/test_trigger.py`:

```python
from typing import Any


async def _async_echo(text: str) -> str:  # pyright: ignore[reportUnusedFunction]
    return f"echo:{text}"


def test_trigger_manager_fire_push():
    import asyncio

    from lythonic.compose.dag_provenance import DagProvenance
    from lythonic.compose.namespace import Dag, Namespace
    from lythonic.compose.trigger import TriggerManager, TriggerStore

    ns = Namespace()
    ns.register(this_module._async_echo, nsref="t:echo")  # pyright: ignore[reportPrivateUsage]

    dag = Dag()
    dag.node(ns.get("t:echo"))
    ns.register(dag, nsref="pipelines:echo_pipe")

    ns.register_trigger(
        name="webhook",
        dag_nsref="pipelines:echo_pipe",
        trigger_type="push",
    )

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        provenance = DagProvenance(Path(tmp) / "provenance.db")
        manager = TriggerManager(namespace=ns, store=store, provenance=provenance)
        manager.activate("webhook")

        result = asyncio.get_event_loop().run_until_complete(
            manager.fire("webhook", payload={"text": "hello"})
        )

        assert result.status == "completed"
        assert result.outputs["_async_echo"] == "echo:hello"

        events = store.get_events("webhook")
        assert len(events) == 1
        assert events[0]["status"] == "completed"
        assert events[0]["run_id"] == result.run_id


def test_trigger_manager_fire_without_start():
    """Push triggers work without calling start()."""
    import asyncio

    from lythonic.compose.namespace import Dag, Namespace
    from lythonic.compose.trigger import TriggerManager, TriggerStore

    ns = Namespace()
    ns.register(this_module._async_echo, nsref="t:echo")  # pyright: ignore[reportPrivateUsage]

    dag = Dag()
    dag.node(ns.get("t:echo"))
    ns.register(dag, nsref="pipelines:echo")

    ns.register_trigger(name="push1", dag_nsref="pipelines:echo", trigger_type="push")

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        manager = TriggerManager(namespace=ns, store=store)
        manager.activate("push1")

        result = asyncio.get_event_loop().run_until_complete(
            manager.fire("push1", payload={"text": "test"})
        )
        assert result.status == "completed"


def test_trigger_manager_fire_deactivated_raises():
    import asyncio

    from lythonic.compose.namespace import Dag, Namespace
    from lythonic.compose.trigger import TriggerManager, TriggerStore

    ns = Namespace()
    ns.register(this_module._async_echo, nsref="t:echo")  # pyright: ignore[reportPrivateUsage]

    dag = Dag()
    dag.node(ns.get("t:echo"))
    ns.register(dag, nsref="pipelines:echo")

    ns.register_trigger(name="push1", dag_nsref="pipelines:echo", trigger_type="push")

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        manager = TriggerManager(namespace=ns, store=store)
        manager.activate("push1")
        manager.deactivate("push1")

        try:
            asyncio.get_event_loop().run_until_complete(
                manager.fire("push1", payload={"text": "test"})
            )
            raise AssertionError("Expected ValueError")
        except ValueError as e:
            assert "not active" in str(e).lower() or "disabled" in str(e).lower()
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `uv run pytest tests/test_trigger.py -k "manager" -v`
Expected: FAIL — `TriggerManager` doesn't exist.

- [ ] **Step 3: Implement TriggerManager with fire()**

Add to `src/lythonic/compose/trigger.py`:

```python
from lythonic.compose.dag_provenance import DagProvenance, NullProvenance
from lythonic.compose.dag_runner import DagRunner, DagRunResult


class TriggerManager:
    """
    Runtime coordinator for triggers. Bridges namespace definitions to
    execution: `activate()` creates DB records, `fire()` runs push triggers,
    `start()`/`stop()` runs a background loop for poll triggers.
    """

    def __init__(
        self,
        namespace: Namespace,
        store: TriggerStore,
        provenance: DagProvenance | NullProvenance | None = None,
    ) -> None:
        from lythonic.compose.namespace import Namespace

        self.namespace = namespace
        self.store = store
        self.provenance = provenance or NullProvenance()
        self._task: asyncio.Task[None] | None = None

    def activate(self, name: str) -> None:
        """Activate a trigger from its namespace definition."""
        td = self.namespace.get_trigger(name)
        self.store.activate(td)

    def deactivate(self, name: str) -> None:
        """Deactivate a trigger."""
        self.store.deactivate(name)

    async def fire(self, name: str, payload: dict[str, Any] | None = None) -> DagRunResult:
        """
        Fire a trigger: run the associated DAG with payload as inputs.
        Records the event in the store. Works whether manager is started or not.
        """
        activation = self.store.get_activation(name)
        if activation is None or activation["status"] != "active":
            raise ValueError(f"Trigger '{name}' is not active")

        dag_nsref = activation["dag_nsref"]
        dag_node = self.namespace.get(dag_nsref)

        # The registered DAG node is an async callable (dag_wrapper from
        # _register_dag) that returns a DagRunResult.
        result = await dag_node(**(payload or {}))

        self.store.record_event(
            trigger_name=name,
            payload=payload,
            run_id=result.run_id,
            status=result.status,
        )
        self.store.update_last_run(name, result.run_id)
        return result
```

Note: The `fire()` method calls the registered DAG's `NamespaceNode` directly (which is the `dag_wrapper` closure from `_register_dag`). The `dag_wrapper` is async and returns a `DagRunResult` via `runner.run()`. This avoids reconstructing the DAG runner — the namespace node already wraps it.

- [ ] **Step 4: Run tests to verify they pass**

Run: `uv run pytest tests/test_trigger.py -k "manager" -v`
Expected: All PASS.

- [ ] **Step 5: Run full test suite and lint**

Run: `make lint && make test`
Expected: All pass.

- [ ] **Step 6: Commit**

```bash
git add src/lythonic/compose/trigger.py tests/test_trigger.py
git commit -m "feat: add TriggerManager with fire() for push triggers"
```

---

### Task 4: TriggerManager — poll triggers with start()/stop()

**Files:**
- Modify: `src/lythonic/compose/trigger.py`
- Test: `tests/test_trigger.py`

- [ ] **Step 1: Write failing tests for poll trigger lifecycle**

Add to `tests/test_trigger.py`:

```python
_poll_counter = 0


def _counting_poll_fn() -> dict[str, str] | None:  # pyright: ignore[reportUnusedFunction]
    import tests.test_trigger as m

    m._poll_counter += 1  # pyright: ignore
    if m._poll_counter >= 2:  # pyright: ignore
        return {"batch": str(m._poll_counter)}  # pyright: ignore
    return None


async def test_trigger_manager_poll_schedule():
    """Poll trigger fires DAG when interval elapses."""
    from lythonic.compose.dag_provenance import DagProvenance
    from lythonic.compose.namespace import Dag, Namespace
    from lythonic.compose.trigger import TriggerManager, TriggerStore
    from lythonic.periodic import stime

    ns = Namespace()
    ns.register(this_module._async_echo, nsref="t:echo")  # pyright: ignore[reportPrivateUsage]

    dag = Dag()
    dag.node(ns.get("t:echo"))
    ns.register(dag, nsref="pipelines:echo")

    ns.register_trigger(
        name="scheduled",
        dag_nsref="pipelines:echo",
        trigger_type="poll",
        interval=Interval.from_string("1h"),
    )

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        provenance = DagProvenance(Path(tmp) / "prov.db")
        manager = TriggerManager(namespace=ns, store=store, provenance=provenance)
        manager.activate("scheduled")

        manager.start()
        # Let the background task run briefly
        await asyncio.sleep(0.1)

        # Should not have fired yet (interval is 1h)
        events = store.get_events("scheduled")
        assert len(events) == 0

        manager.stop()


async def test_trigger_manager_poll_custom_fn():
    """Poll trigger with custom poll_fn fires when fn returns data."""
    from lythonic.compose.namespace import Dag, Namespace
    from lythonic.compose.trigger import TriggerManager, TriggerStore

    import tests.test_trigger as m
    m._poll_counter = 0  # pyright: ignore

    ns = Namespace()
    ns.register(this_module._async_echo, nsref="t:echo")  # pyright: ignore[reportPrivateUsage]

    dag = Dag()
    dag.node(ns.get("t:echo"))
    ns.register(dag, nsref="pipelines:echo")

    ns.register_trigger(
        name="poller",
        dag_nsref="pipelines:echo",
        trigger_type="poll",
        poll_fn=this_module._counting_poll_fn,  # pyright: ignore[reportPrivateUsage]
        interval=Interval.from_string("1s"),
    )

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        manager = TriggerManager(namespace=ns, store=store)
        manager.activate("poller")

        manager.start()
        # Wait for a few poll cycles
        await asyncio.sleep(3.5)
        manager.stop()

        # poll_fn returns None on first call, data on second+
        events = store.get_events("poller")
        assert len(events) >= 1


async def test_trigger_manager_start_stop():
    """Manager starts and stops cleanly."""
    from lythonic.compose.namespace import Namespace
    from lythonic.compose.trigger import TriggerManager, TriggerStore

    ns = Namespace()

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        manager = TriggerManager(namespace=ns, store=store)

        manager.start()
        assert manager._task is not None
        assert not manager._task.done()

        manager.stop()
        await asyncio.sleep(0.1)
        assert manager._task.done() or manager._task.cancelled()


async def test_trigger_manager_deactivated_not_polled():
    """Deactivated poll triggers are not polled."""
    from lythonic.compose.namespace import Dag, Namespace
    from lythonic.compose.trigger import TriggerManager, TriggerStore

    import tests.test_trigger as m
    m._poll_counter = 0  # pyright: ignore

    ns = Namespace()
    ns.register(this_module._async_echo, nsref="t:echo")  # pyright: ignore[reportPrivateUsage]

    dag = Dag()
    dag.node(ns.get("t:echo"))
    ns.register(dag, nsref="pipelines:echo")

    ns.register_trigger(
        name="poller",
        dag_nsref="pipelines:echo",
        trigger_type="poll",
        poll_fn=this_module._counting_poll_fn,  # pyright: ignore[reportPrivateUsage]
        interval=Interval.from_string("1s"),
    )

    with tempfile.TemporaryDirectory() as tmp:
        store = TriggerStore(Path(tmp) / "triggers.db")
        manager = TriggerManager(namespace=ns, store=store)
        manager.activate("poller")
        manager.deactivate("poller")

        manager.start()
        await asyncio.sleep(2.5)
        manager.stop()

        events = store.get_events("poller")
        assert len(events) == 0
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `uv run pytest tests/test_trigger.py -k "poll or start_stop" -v`
Expected: FAIL — `start()` and `stop()` don't exist.

- [ ] **Step 3: Implement start()/stop() and poll loop**

Add these methods to `TriggerManager` in `src/lythonic/compose/trigger.py`:

```python
    def start(self) -> None:
        """Start background asyncio task for polling active poll triggers."""
        if self._task is not None and not self._task.done():
            return
        self._shutdown = asyncio.Event()
        self._task = asyncio.create_task(self._poll_loop())

    def stop(self) -> None:
        """Stop the background poll loop."""
        if self._task is not None and not self._task.done():
            self._shutdown.set()
            self._task.cancel()

    async def _poll_loop(self) -> None:
        """Background loop that checks active poll triggers on their intervals."""
        while not self._shutdown.is_set():
            try:
                active_polls = self.store.get_active_poll_triggers()
                now = time.time()

                for activation in active_polls:
                    config = json.loads(activation.get("config_json") or "{}")
                    interval_str = config.get("interval")
                    if not interval_str:
                        continue

                    interval = Interval.from_string(interval_str)
                    interval_seconds = interval.timedelta().total_seconds()
                    last_run = activation.get("last_run_at") or 0

                    if now - last_run < interval_seconds:
                        continue

                    # Check poll function if present
                    payload: dict[str, Any] | None = None
                    poll_fn_gref = config.get("poll_fn")
                    if poll_fn_gref:
                        from lythonic import GlobalRef

                        fn = GlobalRef(poll_fn_gref).get_instance()
                        result = fn()
                        if result is None:
                            continue
                        payload = result if isinstance(result, dict) else {"data": result}
                    else:
                        # Schedule-only trigger: fire with empty payload
                        payload = {}

                    try:
                        await self.fire(activation["name"], payload=payload)
                    except Exception:
                        import logging
                        logging.getLogger(__name__).exception(
                            "Error firing poll trigger '%s'", activation["name"]
                        )

                await asyncio.sleep(1)
            except asyncio.CancelledError:
                break
            except Exception:
                import logging
                logging.getLogger(__name__).exception("Error in poll loop")
                await asyncio.sleep(1)
```

Also add `self._shutdown: asyncio.Event = asyncio.Event()` to `__init__`.

- [ ] **Step 4: Run tests to verify they pass**

Run: `uv run pytest tests/test_trigger.py -k "poll or start_stop" -v`
Expected: All PASS.

- [ ] **Step 5: Run full test suite and lint**

Run: `make lint && make test`
Expected: All pass.

- [ ] **Step 6: Commit**

```bash
git add src/lythonic/compose/trigger.py tests/test_trigger.py
git commit -m "feat: add TriggerManager.start()/stop() with poll loop"
```

---

### Task 5: Reference page and mkdocs nav

**Files:**
- Create: `docs/reference/compose-trigger.md`
- Modify: `mkdocs.yml`

- [ ] **Step 1: Create reference page**

Create `docs/reference/compose-trigger.md`:

```markdown
# lythonic.compose.trigger

Event-driven DAG trigger system.

::: lythonic.compose.trigger
    options:
      show_root_heading: false
      members:
        - TriggerDef
        - TriggerStore
        - TriggerManager
```

- [ ] **Step 2: Add to mkdocs nav**

In `mkdocs.yml`, add under the API Reference section (after the existing compose references):

```yaml
      - Trigger: reference/compose-trigger.md
```

- [ ] **Step 3: Run lint**

Run: `make lint`
Expected: Pass.

- [ ] **Step 4: Commit**

```bash
git add docs/reference/compose-trigger.md mkdocs.yml
git commit -m "docs: add trigger reference page"
```
